@{
    ViewBag.Title = "Logs";
}
@Styles.Render("~/Content/allLogs.css")
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6">
                <h2>Logs</h2>
                @Html.Partial("_SearchLog")
            </div>
        </div>
        <div class="col">
            <table class="table">
                @foreach (string s in Model)
                {
                    <tbody>
                        <tr>
                            <td><h6>@s</h6></td>
                        </tr>
                    </tbody>
                }
            </table>
        </div>
    </div>
